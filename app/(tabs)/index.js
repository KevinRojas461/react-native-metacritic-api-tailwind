import { Main } from "../../components/Main";

// el punto de entrada va a ser index.js
export default function Index() {
  // trae el comoponente principal de la app
  // export function Main()
  // index es como un header
  return <Main />;
}
